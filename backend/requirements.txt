# Pinned for reproducible 24/7 builds. These are the exact versions running in
# the deployed image (pip freeze, 2026-06). Bump deliberately + re-run the test
# suite before deploying. See docs/TODO.md "Updating safely".
fastapi==0.139.1
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.4
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
apscheduler==3.11.2
structlog==26.1.0
tenacity==9.1.4
yfinance==1.5.1
docker==7.2.0
python-multipart==0.0.32
argon2-cffi==25.1.0
defusedxml==0.7.1

# Transitive deps pinned because the analytics rely on them directly (Black-
# Scholes / market history) and yfinance's resolver can otherwise drift them.
pandas==3.0.3
numpy==2.5.1
websockets==16.0
